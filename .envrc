# Auto-activate uv-managed virtualenv when entering this directory

if [ -d .venv ] && [ -f .venv/bin/activate ]; then
  . .venv/bin/activate
else
  echo "No .venv found. Creating with uv..."
  uv sync
  . .venv/bin/activate
fi
